// Prática 04: Combinações 

for (let contador = 1; contador <= 3; contador++) {
    for (let valor = 1; valor <= 3; valor++) {
        console.log(`${contador}, ${valor}`); 
    }
    console.log('')
}
